import java.net.*;

public class INET_Methods {
    public static void main(String[] args) throws Exception{
        InetAddress inet = InetAddress.getByName("www.ncit.edu.np");
        /*
         inet.getByAddress(url);
         inet.getHostName();
         inet.getHostAddress();
         inet.getLocalHost();
         InetAddress []ad = inet.getAllByName(url) // returns array of InetAddress objects
         for(InetAddress i:ad){
            i.getHostAddress(); //to display ip address associated with that url
         }
         
         */
    }
}
